#include <Arduino.h>
#include "task1000.h"



int counter = 0;  // Counter for seconds

void Task1000() {
    /* 
    counter++;  // Increment every second
    Serial.print("Counter: ");
    Serial.println(counter);

    if (counter % 10 == 0) {  // If counter is a multiple of 5 (5, 10, 15...)
        Serial.println("Toggling LED!");
        digitalWrite(LED_BULTIN, !digitalRead(LED_BUILTIN));  // Turn on LED
    }
    */
}